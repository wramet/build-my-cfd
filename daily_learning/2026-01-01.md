# Governing Equations & OpenFOAM Implementation
## HARDCORE Level - 2026-01-01

---

## 1. Theory: Core Equations & Physics

### 1.1 Conservation of Mass (Continuity Equation)

**สมการการอนุรักษ์มวล (Continuity Equation)**

$$
\frac{\partial \rho}{\partial t} + \nabla \cdot (\rho \mathbf{U}) = 0
$$

**คำอธิบายพจน์:**
- $\rho$ (rho) = **ความหนาแน่น** (density) [kg/m³]
- $\mathbf{U}$ = **สนามความเร็ว** (velocity field) [m/s]
- $\nabla \cdot$ = **ตัวดำเนินการไดเวอร์เจนซ์** (divergence operator)
- $t$ = **เวลา** (time) [s]

สำหรับการไหลแบบ **ไม่อัดตัว** (incompressible flow): $\nabla \cdot \mathbf{U} = 0$

---

### 1.2 Conservation of Momentum (Navier-Stokes Equation)

**สมการการอนุรักษ์โมเมนตัม (Momentum Equation)**

$$
\frac{\partial (\rho \mathbf{U})}{\partial t} + \nabla \cdot (\rho \mathbf{U} \mathbf{U}) = -\nabla p + \nabla \cdot \boldsymbol{\tau} + \rho \mathbf{g}
$$

**คำอธิบายพจน์:**
- $p$ = **ความดัน** (pressure) [Pa]
- $\boldsymbol{\tau}$ (tau) = **เทนเซอร์ความเค้น** (stress tensor) [Pa]
- $\mathbf{g}$ = **ความเร่งเนื่องจากแรงโน้มถ่วง** (gravitational acceleration) [m/s²]
- $\rho \mathbf{U} \mathbf{U}$ = **การพาโมเมนตัม** (momentum convection)

**สำหรับของไหลนิวตัน (Newtonian Fluid):**

$$
\boldsymbol{\tau} = \mu \left[ \nabla \mathbf{U} + (\nabla \mathbf{U})^T \right] - \frac{2}{3}\mu (\nabla \cdot \mathbf{U})\mathbf{I}
$$

- $\mu$ (mu) = **ความหนืด** (dynamic viscosity) [Pa·s]
- $\mathbf{I}$ = **เทนเซอร์เอกลักษณ์** (identity tensor)

---

### 1.3 Conservation of Energy

**สมการการอนุรักษ์พลังงาน (Energy Equation)**

$$
\frac{\partial (\rho e)}{\partial t} + \nabla \cdot (\rho e \mathbf{U}) = -\nabla \cdot \mathbf{q} + \boldsymbol{\tau} : \nabla \mathbf{U} + S_h
$$

**คำอธิบายพจน์:**
- $e$ = **พลังงานภายในต่อหน่วยมวล** (specific internal energy) [J/kg]
- $\mathbf{q}$ = **ฟลักซ์ความร้อน** (heat flux) [W/m²]
- $S_h$ = **แหล่งกำเนิดความร้อน** (heat source) [W/m³]
- $\boldsymbol{\tau} : \nabla \mathbf{U}$ = **การกระจายความร้อนจากความเค้น** (viscous dissipation)

**กฎของฟูริเยร์ (Fourier's Law):**
$$
\mathbf{q} = -k \nabla T
$$

- $k$ = **สัมประสิทธิ์การนำความร้อน** (thermal conductivity) [W/(m·K)]
- $T$ = **อุณหภูมิ** (temperature) [K]

---

### 1.4 Equation of State

**สมการสถานะ (Equation of State)**

สำหรับ **แก๊สอุดมคติ (Ideal Gas):**

$$
p = \rho R T
$$

- $R$ = **ค่าคงที่ของแก๊สเฉพาะ** (specific gas constant) [J/(kg·K)]

สำหรับ **ของไหลที่ไม่อัดตัว (Incompressible Fluid):**

$$
\rho = \text{constant}
$$

---

### 1.5 Transport Equations (General Form)

**สมการขนส่งทั่วไป (General Transport Equation)**

$$
\frac{\partial (\rho \phi)}{\partial t} + \nabla \cdot (\rho \phi \mathbf{U}) = \nabla \cdot (\Gamma_\phi \nabla \phi) + S_\phi
$$

**คำอธิบายพจน์:**
- $\phi$ (phi) = **ตัวแปรขนส่ง** (transported variable) - สามารถเป็นอุณหภูมิ ความเข้มของสาร ฯลฯ
- $\Gamma_\phi$ (Gamma) = **สัมประสิทธิ์การแพร่** (diffusion coefficient)
- $S_\phi$ = **เทอมแหล่งกำเนิด** (source term)

**แต่ละเทอมแทน:**
1. $\frac{\partial (\rho \phi)}{\partial t}$ = **การเปลี่ยนแปลงตามเวลา** (transient term)
2. $\nabla \cdot (\rho \phi \mathbf{U})$ = **การพา** (convection term)
3. $\nabla \cdot (\Gamma_\phi \nabla \phi)$ = **การแพร่** (diffusion term)
4. $S_\phi$ = **แหล่งกำเนิด/ตัวรับ** (source/sink term)

---

## 2. OpenFOAM Class Hierarchy & Implementation

### 2.1 Core Field Classes

OpenFOAM represents governing equations through field classes that store and manipulate data at mesh points/cells.

```
GeometricField<Type, PatchField, GeoMesh>
│
├── volScalarField  (scalar field on cell centers)
│   └── Example: p (pressure), T (temperature), rho (density)
│
├── volVectorField  (vector field on cell centers)
│   └── Example: U (velocity)
│
├── surfaceScalarField  (scalar field on cell faces)
│   └── Example: phi (flux field)
│
└── pointScalarField  (scalar field on mesh points)
```

**Reference Files:**
- `$FOAM_SRC/finiteVolume/fields/volFields/volScalarField/volScalarField.H`
- `$FOAM_SRC/finiteVolume/fields/volFields/volVectorField/volVectorField.H`
- `$FOAM_SRC/finiteVolume/fields/surfaceFields/surfaceScalarField/surfaceScalarField.H`

---

### 2.2 Dimensioned Quantities

All physical quantities in OpenFOAM carry dimensional information.

```
dimensionSet
│
├── dimensionedScalar  (scalar with dimensions)
│   └── Example: nu (kinematic viscosity) [m²/s]
│
├── dimensionedVector  (vector with dimensions)
│   └── Example: g (gravity) [m/s²]
│
└── dimensionedTensor  (tensor with dimensions)
```

**Reference Files:**
- `$FOAM_SRC/OpenFOAM/dimensionSet/dimensionSet.H`
- `$FOAM_SRC/OpenFOAM/dimensionedTypes/dimensionedScalar/dimensionedScalar.H`

---

### 2.3 Matrix Classes & Equation Solvers

The discretized equations form linear systems solved through matrix classes.

```
lduMatrix  (lower-diagonal-upper matrix for unstructured meshes)
│
├── fvMatrix  (finite volume matrix)
│   ├── solve()  - solves the linear system
│   ├── A()      - returns the diagonal
│   ├── H()      - returns the source term
│   └── Apsi()   - matrix-vector multiplication
│
└── lduSolver
    ├── GAMG  (Geometric-Algebraic Multi-Grid)
    ├── PCG   (Preconditioned Conjugate Gradient)
    ├── PBiCG (Preconditioned Bi-Conjugate Gradient)
    └── smoothSolver
```

**Reference Files:**
- `$FOAM_SRC/finiteVolume/fields/fvPatchFields/fvPatchField/fvPatchField.H`
- `$FOAM_SRC/OpenFOAM/matrices/lduMatrix/lduMatrix.H`
- `$FOAM_SRC/OpenFOAM/matrices/lduMatrix/solvers/lduSolver.H`

---

### 2.4 Discretization Schemes

OpenFOAM uses template-based schemes for spatial and temporal discretization.

```
fv::schemes
│
├── ddtSchemes  (Time derivatives)
│   ├── Euler
│   ├── backward
│   ├── CrankNicolson
│   └── localEuler
│
├── gradSchemes  (Gradient calculation)
│   ├── Gauss
│   ├── leastSquares
│   └── fourth
│
├── divSchemes  (Divergence terms)
│   ├── Gauss upwind
│   ├── Gauss linear
│   ├── Gauss limitedLinearV 1
│   └── Gauss vanLeerV
│
├── laplacianSchemes  (Laplacian terms)
│   ├── Gauss linear corrected
│   ├── Gauss linear uncorrected
│   └── cellLimited Gauss linear 1
│
└── interpolationSchemes  (Face interpolation)
    ├── linear
    ├── cubic
    └── upwind
```

**Reference Files:**
- `$FOAM_SRC/finiteVolume/finiteVolume/ddtSchemes/`
- `$FOAM_SRC/finiteVolume/finiteVolume/gradSchemes/`
- `$FOAM_SRC/finiteVolume/finiteVolume/divSchemes/`
- `$FOAM_SRC/finiteVolume/finiteVolume/laplacianSchemes/`

---

### 2.5 Boundary Conditions

Boundary conditions are implemented as derived classes of `fvPatchField`.

```
fvPatchField<Type>
│
├── fixedValueFvPatchField
│   └── Dirichlet BC: value specified on boundary
│
├── fixedGradientFvPatchField
│   └── Neumann BC: gradient specified on boundary
│
├── zeroGradientFvPatchField
│   └── Zero gradient (∂φ/∂n = 0)
│
├── mixedFvPatchField
│   └── Robin BC: combination of value and gradient
│
├── inletOutletFvPatchField
│   └── Switches between inlet and outlet based on flux direction
│
└── calculatedFvPatchField
    └── Value calculated from other fields
```

**For Velocity (U):**
```
fixedValueFvPatchField<vector>
│
├── uniformFixedValue
├── surfaceNormalFixedValue
├── flowRateInletVelocity
├── pressureInletVelocity
└── zeroGradient
```

**For Pressure (p):**
```
fixedValueFvPatchField<scalar>
│
├── uniformFixedValue
├── totalPressure
├── fixedFluxPressure
└── zeroGradient
```

**Reference Files:**
- `$FOAM_SRC/finiteVolume/fields/fvPatchFields/basic/`
- `$FOAM_SRC/finiteVolume/fields/fvPatchFields/derived/`
- `$FOAM_SRC/finiteVolume/fields/fvPatchFields/constraint/`

---

### 2.6 Turbulence Modeling Classes

Turbulence models follow a hierarchical structure for RANS/LES simulations.

```
turbulenceModel
│
├── RASModel  (Reynolds-Averaged Simulation)
│   ├── kEpsilon
│   ├── kOmegaSST
│   ├── SpalartAllmaras
│   └── LRR (Reynolds Stress Model)
│
├── LESModel  (Large Eddy Simulation)
│   ├── Smagorinsky
│   ├── kEqn (One-equation LES)
│   └── dynamicLagrangian
│
└── laminar  (Laminar flow - no turbulence model)
```

**Reference Files:**
- `$FOAM_SRC/turbulenceModels/`
- `$FOAM_SRC/turbulenceModels/incompressible/RAS/`
- `$FOAM_SRC/turbulenceModels/incompressible/LES/`

---

### 2.7 Mesh & Topology Classes

The mesh structure supports finite volume discretization.

```
polyMesh
│
├── points_  (vertex coordinates)
├── faces_   (face definitions)
├── cells_   (cell-face connectivity)
├── owner_   (face-owner addressing)
├── neighbour_  (face-neighbor addressing)
│
└── fvMesh  (finite volume mesh)
    ├── Sf()  - face area vectors
    ├── magSf()  - face area magnitudes
    ├── C()   - cell centers
    ├── V()   - cell volumes
    └── delta()  - cell distance vectors
```

**Reference Files:**
- `$FOAM_SRC/meshes/polyMesh/polyMesh.H`
- `$FOAM_SRC/finiteVolume/fvMesh/fvMesh.H`

---

### 2.8 Thermophysical Models

For energy equations and equation of state.

```
basicThermo
│
├── heThermo  (energy-based thermodynamics)
│   ├── heRhoThermo  (compressible)
│   └── hePsiThermo  (low-Mach compressible)
│
├── rhoThermo  (density-based)
└── psiThermo  (compressibility-based)
```

**Transport Models:**
```
transportModel
│
├── constTransport  (constant properties)
├── sutherlandTransport  (temperature-dependent viscosity)
└── polynomialTransport  (polynomial coefficients)
```

**Reference Files:**
- `$FOAM_SRC/thermophysicalModels/basic/`
- `$FOAM_SRC/thermophysicalModels/specie/`

---

## 3. Code Walkthrough

### 3.1 UEqn.H

ไฟล์ `UEqn.H` ใช้สำหรับสร้างสมการโมเมนตัม (Momentum Equation) ใน OpenFOAM solver ต่างๆ เช่น `simpleFoam`, `pimpleFoam` ฯลฯ

**โครงสร้างพื้นฐาน:**

```cpp
// สร้างสมการเมทริกซ์สำหรับสนามความเร็ว U
fvVectorMatrix UEqn
(
    fvm::ddt(U)                              // เทอมอนุพันธ์เวลา (transient)
  + fvm::div(phi, U)                         // เทอมการพา (convection)
  + turbulence->divDevReff(U)                // เทอมการแพร่ของความเค้น (viscous term)
 ==
    fvOptions(U)                             // เทอมแหล่งกำเนิด (source terms)
);

// ผ่อนคลายสมการ (relaxation) เพื่อเสถียรภาพเชิงตัวเลข
UEqn.relax();

// แก้สมการโดยไม่รวมเทอมความดัน (pressure-free solution)
solve(UEqn == -fvc::grad(p));
```

**คำอธิบายเทอมสำคัญ:**

| เทอม | ความหมาย | การดำเนินการ |
|-------|-----------|----------------|
| `fvm::ddt(U)` | ∂U/∂t | Implicit - ใช้เมทริกซ์ |
| `fvm::div(phi, U)` | ∇·(UU) | Implicit - ใช้เมทริกซ์ |
| `turbulence->divDevReff(U)` | ∇·τ_eff | Implicit - ความเค้นจากความปั่น |
| `fvc::grad(p)` | -∇p | Explicit - คำนวณโดยตรง |
| `fvOptions(U)` | S_U | Explicit/Implicit - แหล่งกำเนิด |

**การแยกสมการ (Splitting):**

OpenFOAM ใช้ **PISO/SIMPLE algorithm** โดยแยกสมการโมเมนตัมและความดัน:

1. **Predictor Step:** แก้ UEqn โดยไม่รวม ∇p
2. **Pressure Equation:** แก้สมการความดันจาก ∇·U = 0
3. **Corrector Step:** แก้ไข U ด้วย ∇p ใหม่

**ตัวอย่างเพิ่มเติม (สำหรับ compressible flow):**

```cpp
// กรณีของไหลที่อัดตัว (compressible)
fvVectorMatrix UEqn
(
    fvm::ddt(rho, U)                         // รวมความหนาแน่น
  + fvm::div(rhoPhi, U)                      // flux ถ่วงมวล
  + turbulence->divDevRhoReff(U)             // viscous term (rho-weighted)
 ==
    fvOptions(rho, U)
);
```

**Reference Files:**
- `$FOAM_APP/solvers/incompressible/simpleFoam/UEqn.H`
- `$FOAM_APP/solvers/incompressible/pimpleFoam/UEqn.H`
- `$FOAM_SRC/finiteVolume/fields/fvPatchFields/`

---

## 4. Dictionary Analysis & Configuration

<!-- PLACEHOLDER_DICT -->

---

## 5. Hands-on: Practical Tasks & Coding

<!-- PLACEHOLDER_TASKS -->

---

## 6. Concept Checks

<!-- PLACEHOLDER_CHECKS -->

---

## Recommended Reading

- OpenFOAM User Guide: https://www.openfoam.com/documentation/user-guide
- OpenFOAM Programmer's Guide: https://doc.openfoam.com/
- CFD Online Forum: https://www.cfd-online.com/Forums/openfoam/

