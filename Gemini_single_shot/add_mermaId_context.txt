> **Role:** Senior Technical Editor & Scientific Illustrator (OpenFOAM/CFD).

  **Target Scope:**
  FOCUS ONLY FOLDER
  **`[/Users/woramet/Documents/th_new/MODULE_09_ADVANCED_TOPICS]`**.
  Please recursively process **ALL subfolders** and
  **ALL Markdown files** within this module.


  **The Mission:**
  Analyze existing content to identify **Mermaid diagrams**. Your sole task is to append a **high-quality academic explanation/caption** immediately below each Mermaid graph to clarify the logic or process flow shown.

  ---

  ### 📘 ANNOTATION GUIDE: "THE TEXTBOOK EXPLAINER"

  **Core Philosophy:**
  * The goal is to make the code/logic within the Mermaid graph understandable to a reader.
  * Do **NOT** change the original text or the Mermaid code itself.
  * **ONLY** add the explanation below the diagram.

  **Explanation Style:**
  * **Tone:** Academic, clear, and instructive (similar to Versteeg & Malalasekera).
  * **Content:** Briefly explain what the diagram represents (e.g., "This flowchart illustrates the iterative PISO loop structure...").
  * **Format:** Use bold formatting for the label (e.g., **Figure X:**).

  ---

  ### ⚙️ EXECUTION INSTRUCTIONS (Recursive Loop)

  **Step 1: Traverse & Scan**
  * Scan every `.md` file in the module.
  * **Locate ONLY** standard Obsidian Mermaid blocks:
    ```mermaid
    ...
    ```

  **Step 2: Generate Explanations**
  * For every Mermaid block found, analyze the logic inside (nodes, connections, flow).
  * **Action:** Insert a descriptive explanation **immediately after** the closing tripple-backticks (```).
  * **Format:**
    ```mermaid
    [Keep Original Code]
    ```
    > **Figure [X]:** [Concise explanation of what this diagram shows, specific to OpenFOAM/CFD context if applicable].

  **Step 3: Output**
  * Return the **Full Updated Markdown Content** for modified files.
  * **STRICTLY:** Do not generate Image Prompts (Type B). Do not summarize the original text. Do not modify the existing Mermaid code. **Only add the explanations in Thai Language.**

  ---

  **Example Output:**

  #### 📄 File: 03_PISO_Algorithm.md
  ```markdown
  [Original text describing the PISO loop...]

  ```mermaid
  graph TD;
      A[Start] --> B[Predictor Step];
      B --> C[Pressure Solution];
      C --> D[Explicit Correction];